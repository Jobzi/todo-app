/* eslint-disable no-unused-vars */
import React from 'react'
import styled from 'styled-components'

const Pl = styled.p`
    cursor: pointer;
    font-size: x-large;
    letter-spacing: 1rem;
    &:hover {
    color:#06f;
  }
`

export default Pl
